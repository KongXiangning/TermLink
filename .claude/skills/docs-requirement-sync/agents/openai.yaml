interface:
  display_name: "Docs Requirement Sync"
  short_description: "Implement from REQ docs with REQ + PLAN + CR sync and plan coverage reporting"
  default_prompt: "Use $docs-requirement-sync to implement from a REQ document, update the linked PLAN and CR in the same batch, and explicitly report which plan items were implemented."
