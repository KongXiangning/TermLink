<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/shell_root_drawer"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/shell_main_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <LinearLayout
            android:id="@+id/shell_top_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#1F1F1F"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingStart="8dp"
            android:paddingTop="6dp"
            android:paddingEnd="8dp"
            android:paddingBottom="6dp">

            <ImageButton
                android:id="@+id/btn_open_sessions_drawer"
                android:layout_width="38dp"
                android:layout_height="38dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/sessions_panel_button"
                android:padding="6dp"
                android:src="@drawable/ic_sessions_24"
                android:tint="@android:color/white" />

            <ImageButton
                android:id="@+id/btn_back_terminal"
                android:layout_width="38dp"
                android:layout_height="38dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/settings_back_button"
                android:padding="6dp"
                android:src="@drawable/ic_arrow_back_24"
                android:tint="@android:color/white"
                android:visibility="gone" />

            <TextView
                android:id="@+id/shell_status_text"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:layout_weight="1"
                android:paddingHorizontal="6dp"
                android:text="@string/terminal_state_idle"
                android:textColor="#FFFFFF"
                android:textSize="12sp" />

            <ImageButton
                android:id="@+id/btn_open_settings"
                android:layout_width="38dp"
                android:layout_height="38dp"
                android:layout_marginStart="6dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/settings_open_button"
                android:padding="6dp"
                android:src="@drawable/ic_settings_24"
                android:tint="@android:color/white" />
        </LinearLayout>

        <FrameLayout
            android:id="@+id/shell_fragment_container"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />
    </LinearLayout>

    <FrameLayout
        android:id="@+id/shell_sessions_drawer_container"
        android:layout_width="340dp"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:background="#121212" />
</androidx.drawerlayout.widget.DrawerLayout>
